[tool.ruff]
target-version = "py313"
line-length = 100
exclude = [
    ".agent",
    ".git",
    ".mypy_cache",
    ".pytest_cache",
    ".ruff_cache",
    "__pycache__",
    "data",
    "logs",
    "quarantine",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
